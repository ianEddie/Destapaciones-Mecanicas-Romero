---
import { FOOTER_DATA } from '@/constants/footer-contact.astro'
import heroImage from '@/assets/destapacion-de-cañeria.webp'
---

<section
  id='home'
  class='sm:min-h-screen min-h-[95vh] w-full grid sm:grid-cols-2 gap-x-10 items-center sm:px-20 px-0 font-primary'
>
  <article
    class='sm:h-auto h-full flex flex-col items-start sm:justify-center justify-around sm:gap-y-14 sm:pt-0 pt-20'
  >
    <!-- Title -->
    <div class='flex flex-col text-center sm:w-auto w-full sm:gap-y-0 gap-y-3'>
      <h1 class='text-title flex flex-col gap-1 font-medium'>
        Destapaciones Mecánicas
      </h1>
      <span
        class='text-[1.3rem] uppercase tracking-widest text-stone-600 font-secondary'
        >Romero</span
      >
    </div>
    <!-- Text -->
    <p
      class='font-text text-pretty sm:text-desktop-text text-mobile-text sm:max-w-[90%] w-full sm:px-0 px-7 text-black font-light'
    >
      Somos una empresa familiar con más de <strong class='font-medium'
        >15 años de experiencia</strong
      > en el rubro. Nos especializamos en <strong class='font-medium'
        >Destapaciones Mecánicas</strong
      > , ya que contamos con las mejores herramientas del mercado para realizar
      nuestro trabajo de la mejor manera.
    </p>
    <!-- Nav -->
    <nav
      class='sm:px-0 px-7 flex sm:flex-row flex-col sm:justify-between justify-around items-start gap-y-7 sm:w-[90%] w-full'
      aria-label='hero navigation'
    >
      {
        FOOTER_DATA.map((item) => (
          <button class='flex items-center gap-x-2 w-fit hover:scale-105 hover:-translate-y-1 transition-all duration-300'>
            <item.icon class='sm:size-[23px] size-5 text-neutral-900' />
            <span class='text-neutral-900/90 sm:text-[16px] text-sm font-semibold'>
              {item.value}
            </span>
          </button>
        ))
      }
    </nav>
  </article>
  <picture class='sm:flex hidden justify-end'>
    <img
      src={heroImage.src}
      alt='Destapacion de cañeria'
      title='Destapaciones de cañerias'
      width={heroImage.width}
      height={heroImage.height}
      class='rounded-lg shadow-custom grayscale-[15%] aspect-auto'
    />
  </picture>
</section>
